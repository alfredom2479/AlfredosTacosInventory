extends layout 

block content 
  h1= title 

  ul 
    each val in category_list 
      li 
        a(href=val.url) #{val.name}
        p #{val.description}

    else 
      li There are no categories in your inventory